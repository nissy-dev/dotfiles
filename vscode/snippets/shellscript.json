{
  "Initialize shell scripts.": {
    "prefix": ["init", "shell-init"],
    "body": [
      "#!/usr/bin/env bash",
      "# -u: Fail on when existing unset variables",
      "# -e -o pipefail: Fail on when happening command errors",
      "set -ueo pipefail",
      "$0"
    ],
    "description": "Initialize shell scripts."
  }
}
